apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bulb.fullname" . }}-test-connection"
  labels:
    {{- include "bulb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bulb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
